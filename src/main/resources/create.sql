CREATE TABLE IF NOT EXISTS `database`.PLAYERDC (
    player_id VARCHAR(100) NOT NULL,
    connect_code VARCHAR(100) NOT NULL,
    connected VARCHAR(20) NOT NULL,
    discord_id VARCHAR(100) NOT NULL
    );